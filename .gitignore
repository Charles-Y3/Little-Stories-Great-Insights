# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/dist

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel

# local scratch / tooling (do not commit)
scripts/tmp/
.claude/

# original story artwork masters live in assets-src/ and ARE committed
# (see docs/AUTHORING.md) — do not add assets-src/ here.

# pre-project source dump: byte-for-byte duplicate of assets-src/, just under
# the original (non-ASCII) filenames. assets-src/ is the tracked source of
# truth; this folder is kept on disk for reference only, not versioned.
/images
